// let and const in JS and Temporal dead zone

// TDZ(Temporal Dead Zone) - TDZ is the period where the variables were not accessible until
// the moment the computer initializes it with a value.

console.log(a);

let a=10;
var b=100;
console.log(b);
